# DECISIONS (ADR-lite) — newest last

| ID | Date | Decision | Reason | Alternatives rejected | Consequence |
|----|------|----------|--------|-----------------------|-------------|
| D1 | 2026-08-21 | Modular monolith, package-by-feature (`com.bookaura.<feature>`) | Assignment 1 constraint; demo must be explainable | microservices, layer-by-package | One deployable; clear module boundaries |
| D2 | 2026-08-21 | Real **JDK 17** (`F:\tools\jdk-17.0.20+8`) pinned via `JAVA_HOME` for all builds | User requirement; avoids JDK24 `--release` subtleties | compile with JDK 24 `--release 17` | README documents path; CI uses JDK 17 |
| D3 | 2026-08-21 | Maven only + committed **Maven Wrapper**; local repo relocated to `F:\m2\repository` (C: drive full) via `MAVEN_ARGS`/`-Dmaven.repo.local` | Reproducible builds without global Maven; C: has <1GB free | Gradle; committing machine path into `.mvn/maven.config` | Devs run `mvnw`; this machine sets repo to F: |
| D4 | 2026-08-21 | Access token **in memory** (15 min), refresh token **HttpOnly + SameSite=Lax cookie**, rotation + family revocation on reuse, logout revokes session + blacklists `jti` | XSS can't steal memory/HttpOnly tokens; logout must actually kill tokens (JWT alone is stateless) | localStorage (XSS-exfiltratable); stateless-only JWT (no real logout) | Needs silent refresh on reload; CSRF analysis documented in AUTH_FLOW.md; per-request jti check (indexed lookup) |
| D5 | 2026-08-21 | Borrow concurrency = **atomic conditional update** (`SET available = available - 1 ... WHERE available > 0`, affected-rows decide) | One understandable mechanism; DB is the arbiter; no lock combinations | pessimistic lock (simpler but lock held longer), optimistic retry (extra complexity), parallelStream in TX (forbidden) | Explainable to mentor; concurrency test with 2 threads on last copy |
| D6 | 2026-08-21 | Soft delete: `Book.active/deleted_at`, `UserAccount.status`; `Loan` immutable (never deleted) | Audit trail; FK safety with loan history | hard delete | Search filters default to active; unique constraints designed accordingly |
| D7 | 2026-08-21 | Entity identity: UUID surrogate PK; `equals/hashCode` on PK only with **constant hashCode**; no Lombok `@Data`; no blind `CascadeType.ALL` | Email is mutable → forbidden as identity; lazy relations break naive equals/hashCode | `@Data`, natural-key equals | Documented in ERD.md; safe in Sets before persist |
| D8 | 2026-08-21 | Email: `EmailSender` interface; local = Mailpit; demo/prod = **Brevo SMTP** via env vars | Demo must send real verification email; no secrets in repo | hardcoding one provider | Same infra reused for password reset & change-email |
| D9 | 2026-08-21 | SMS: `SmsSender` interface + `FakeSmsSender` (console, **local-only, clearly labeled**) | No SMS budget now; OTP business flow fully implemented regardless | paying for Twilio now; claiming phone OTP "production-ready" | Real gateway later behind interface without touching business code |
| D10 | 2026-08-21 | Roles seeded by **Liquibase**; `admin/admin` seeded by `@Profile("local")` `CommandLineRunner` with real BCrypt `PasswordEncoder` | Liquibase can't portably BCrypt-hash; spec: never hardcode prod credential | hardcoded hash in changelog; seeding admin in prod | Admin exists only in local/demo |
| D11 | 2026-08-21 | Liquibase is the only schema source; `spring.jpa.hibernate.ddl-auto=validate` | Empty DB must boot from zero; drift detection | `update`/`create-drop` | Schema changes = new changesets only |
| D12 | 2026-08-21 | Maintenance mode: DB-backed flag + cached in memory (invalidated on toggle); **whitelist**: admin config endpoint + `/actuator/health` | Avoids admin lockout deadlock; no per-request DB hit | cache with TTL (stale window); blocking everything (deadlock) | Exception documented in code + docs |
| D13 | 2026-08-21 | Docs-lite strategy: 8 essential files, evolve alongside code; no speculative docs | 4-day deadline | full 14-file doc set up front | Docs updated per slice; demo docs (DEMO_SCRIPT, MENTOR_QA) at the end |
| D14 | 2026-08-21 | Supabase = managed PostgreSQL (+ later Storage) only; **never** Supabase Auth; frontend never calls Supabase tables | Assignment must demonstrate Spring Security + JWT | Supabase Auth shortcut | All business data: React → Spring Boot → Postgres |
| D15 | 2026-08-21 | Priority ladder P0-A (31 items) → P0-B (Google OAuth → email OTP/change-email → phone OTP mock → Facebook) → P2 (Shelf Aura → 3D → real SMS → AI); cut bottom-up | 4-day deadline; never sacrifice Book/Member/Borrow/Auth for wow features | equal priority | Scope cuts follow this exact order |
| D16 | 2026-08-21 | Unverified login returns **401** with code `EMAIL_NOT_VERIFIED` | Authentication-level failure; frontend can offer "resend verification" | 403 (authenticated-but-forbidden semantics don't fit a failed login) | Documented in AUTH_FLOW.md + OpenAPI |
| D17 | 2026-08-21 | Refresh-token **family revocation runs in `REQUIRES_NEW`** (`RefreshFamilyRevoker`); login identifier = email-first, then phone | Bug found by integration test: reuse detection threw 401 → TX rollback → family revocation was lost. Security response must persist even when the request fails. Identifier: seeded local admin (`admin`) is not an email and not a phone | rollback-the-family (broken), marking reuse as committed-then-throw in same TX; classifying identifier by "contains @" (breaks `admin`) | Proven by `refresh_rotatesToken_andReuseOfOldTokenRevokesWholeFamily` |
| D18 | 2026-08-23 | Catalog search uses composable Spring Data `Specification`; page size 1–10; sort contract `field:direction,...` with allowlist; `@BatchSize(50)` for page relations | SQL-side filtering/paging prevents unbounded `findAll`; allowlist prevents invalid/property probing; batch loading avoids 2N lazy queries without collection-fetch pagination | Java-side filtering; raw client sort property; collection fetch join with pagination | Public search forces `active=true`; ADMIN search may filter active/inactive; multi-sort limited to 5 fields |
| D19 | 2026-08-23 | CSV = Apache Commons CSV streaming parser, exact 7-column header, strict `<5 MiB`, max 10k rows, all-or-nothing TX; duplicate ISBN via `Set`, relation/ISBN bulk lookup | Reliable quoted CSV parsing; bounded memory; O(n) duplicate lookup; useful row errors; no DB calls per row | manual `split(',')` (breaks quoted values); read entire file bytes; partial commits; O(n²) duplicate scans | Test-only hook throws after `saveAll+flush` to prove books/authors/categories rollback; no production failure endpoint |
| D20 | 2026-08-23 | Borrow/return concurrency uses only conditional atomic updates + affected-row count; loan period = 14 days; PostgreSQL partial unique active-loan index | Final-copy race is decided atomically by DB; return's NULL→timestamp transition makes duplicate return safe; unique index closes concurrent duplicate-active-loan race | pessimistic locks; optimistic version+retry; mixing all three strategies | Losing transactions affect 0 rows or hit unique index and roll back; integration test starts two real transactions for one final copy |
| D21 | 2026-08-23 | Member borrowed-title and role filters avoid root `DISTINCT`; borrowed title uses correlated `EXISTS`; search dates parse strictly as `uuuu/MM/d` | PostgreSQL rejects `SELECT DISTINCT member-columns ORDER BY user.email`; EXISTS removes duplicates at the source and gives valid pageable/count queries. Strict resolver rejects malformed/impossible dates | join + distinct (failed with nested sort); Java post-filter; lenient date parser | Seven filters remain composable SQL Specifications; regression test combines all filters and nested sort |
| D22 | 2026-08-23 | Integration tests use one singleton PostgreSQL container per Maven JVM + `DynamicPropertySource` | Spring context cache can outlive a JUnit inherited `@Container`; restarting/stopping it left cached DataSources on dead random ports | `@DirtiesContext` on every class; one container per class | Full suite shares stable DB endpoint; tests use unique data and real transactions; Ryuk/JVM shutdown cleans container |
| D23 | 2026-08-23 | Maintenance flag persists in singleton DB row and is cached in `AtomicBoolean`; cache changes only `afterCommit`; filter exceptions = health, ADMIN control path, OPTIONS | No per-request config query; failed toggle cannot desync cache/DB; narrow path avoids lockout and supports CORS | DB query each request; cache TTL (stale); whitelist all auth endpoints; block every endpoint (deadlock) | Control path still goes through Security + `@PreAuthorize`; a currently valid ADMIN token is required while ON |
| D24 | 2026-08-23 | HTTP logs use bounded JSON caching/redaction; auth/OAuth/multipart/infra bodies suppressed; `@LogOperation` AOP logs no args/results | Mentor requires request/response + AOP logging but secrets/PII must not leak; query strings can contain verification/OAuth codes | log all bodies/headers (unsafe); metadata only (less demo evidence); business logic in aspects | Max logged JSON 2k chars; response caching limited to bounded business APIs; passwords/tokens/codes/member PII redacted; operation timing remains cross-cutting only |
| D25 | 2026-08-23 | React session state is memory-only; one shared Axios refresh promise retries a 401 once; React Query owns server state; catalog uses URL state; ADMIN routes lazy-load | Preserves HttpOnly/token threat model, prevents refresh storms, keeps Back/deep links correct and removes 500k main-chunk warning | local/sessionStorage access JWT; parallel refresh per request; global store for API pages; purely local filters; raise bundle warning threshold | Reload needs refresh cookie; failed refresh clears all identity/cache; backend remains role authority; ADMIN chunks load on first visit |
| D26 | 2026-08-23 | Google OIDC callback redirects a 60s one-time code (SHA-256 at rest, atomic consume), then exchange creates normal JWT/refresh session; stable provider subject stored separately; configuration is credential-conditional | JWT/provider token in URL leaks to history/referrers/logs; email alone is mutable; assignment must boot/test without real Google secrets | JWT in redirect; frontend Google SDK; provider token as app token; always-required credentials; permanent OAuth HTTP session | Spring validates state/nonce/signature/verified email; transient handshake session invalidated; React removes code from history; real consent smoke awaits credentials |
| D27 | 2026-08-23 | Change-email confirms the latest authenticated user/purpose OTP; wrong attempts commit in `REQUIRES_NEW`; correct code atomically marks consumed; user UUID/JWT `sub` stays stable | Global six-digit hash lookup risks collisions; throwing 400 rolls ordinary attempt increments back; concurrent confirms need one winner; email is mutable | email link without OTP lifecycle; raw code storage; update email before verification; attempt counter in failed outer TX | Ten-minute code, 60s cooldown, five attempts; new email only in token target until confirm; existing sessions remain valid and frontend syncs UserSummary |
| D28 | 2026-08-23 | Phone OTP request is enumeration-safe; local/test fake sender stores code only in memory; local retrieval is ADMIN-only; no OTP console log; confirm issues standard session | Demo needs inspectable fake delivery but observability forbids raw OTP logs; public outbox would leak authentication secrets | console-log OTP; public dev endpoint; store raw code in DB; claim real SMS gateway | Missing/disabled/cooldown all return generic 200; local admin can retrieve for demo; prod explicitly reports gateway unavailable; real SMS remains P2 |
